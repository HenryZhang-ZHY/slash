---
title: 使用 Test Engine 控制台
description: "从 Web UI 创建 suites、查看 executions 并管理测试状态。"
sidebar:
  order: 5
---

在 Slash 控制台打开 `/tests`。该页面是 Test Engine 管理 API 的用户界面；状态存储在
服务端，而不是只存在浏览器中。

## 管理 Suites

使用 suite 选择器查看 execution 总数、通过率以及 muted 或 skipped 数量。创建 suite
需要 owner、repository 和 suite key。Slash 创建 suite 后，再明确创建它的第一个
collection token。

## 查找并检查测试

按状态过滤，按名称、文件或 label 搜索，并按最近时间、名称、时长或失败数排序。选择
测试后可以查看元数据、聚合历史和单条 execution。

## 修改测试状态

你可以 enable、mute 或 skip 测试。Mute 会让测试继续提供观测数据，同时防止失败阻塞
拉取请求；skip 是停止产生新 execution 证据的手动决策。

## 管理 Collection Tokens

为每个 CI 采集器或 runner 创建一个具名 token。明文凭据仅在签发时显示，应直接保存
到负责上传结果的 CI 系统。Token 默认永不过期；也可以选择 30、90、365 天或自定义
到期日期。

一个 suite 可以有多个有效 token。**轮换**会创建具名的替代 token，但不会吊销旧
token，因此可以安全切换 CI，并在旧 token 不再有流量后手动吊销。控制台为每个 token
保留非 secret 的创建、到期、最后使用和吊销元数据。

状态语义见[测试与隔离](/zh-hans/test-engine/concepts/)，精确请求和错误见
[管理 API](/zh-hans/api/test-engine/)。
