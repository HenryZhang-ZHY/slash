---
title: Web 控制台
description: "从 Slash 的 Web UI 登录、创建团队并管理 Test Engine。"
sidebar:
  order: 8
---

Slash 从与 API 相同的来源提供一个 Web 控制台（React SPA）。它涵盖账户 onboarding
和 Test Engine 控制面板。控制台没有任何仅客户端的数据 —— 每个屏幕都调用有文档的
HTTP API，所以控制台能做的任何事通过 API 也都能做。

## 登录与注册

打开控制台根路径（例如 `https://slash.example.com/`）—— 未认证访问会重定向到
`/login`。登录页在 **Sign in** 和 **Create your account** 之间切换：

- **Create your account** —— 用邮箱和至少 8 字节的密码注册（`POST
  /api/auth/register`）。注册后立即登录。
- **Sign in** —— 用现有账户登录（`POST /api/auth/login`）。
- **Sign in with GitHub** —— 按稳定 GitHub 身份登录，或创建一个不带邮箱密码凭据的
  Slash 账户。Slash 不读取 GitHub 邮箱，也不会按邮箱自动合并账户。

两者都设置 `HttpOnly` 会话 cookie；控制台永远看不到密码。

## Onboarding：创建你的第一个团队

登录后，控制台重定向到 `/onboarding`，要求输入**团队名**和**团队 slug**（小写
字母、数字和连字符，最多 63 个字符）。创建团队调用 `POST /api/teams`，在一个事务里
为你创建 home 组织（如果你还没有）并把你设为团队的 maintainer。团队用于组织 Test
Engine 等 Slash 原生产品；slash command 权限由 GitHub 仓库权限独立控制。

## 仪表盘

工作区首页（`/`）显示概览：你的团队数量、Test Engine 套件/测试用例/执行数量、
团队列表，以及进入 Test Engine 控制台的快捷方式。

## Settings 与 GitHub 连接

`/settings` 从 `GET /api/auth/me` 读取真实连接状态。邮箱密码用户可以通过
`POST /api/auth/github/connect` 显式连接 GitHub；已连接用户会看到 GitHub login。

同一页面允许每个用户为 Agent 和 REST API 客户端创建、查看和撤销个人 Access Token。
明文只在创建后显示一次；列表会展示创建、最近使用和到期状态，不会再次暴露凭据。
详见[个人 Access Token](/zh-hans/api/access-tokens/)。

## Test Engine 控制台

`/tests` 页面是 Test Engine 控制面板。你可以：

- **列出并选择套件** —— 选一个套件查看它的测试、通过率和 muted/skipped 数量。
- **创建套件** —— 这与 API 暴露的创建套件端点相同，并返回一个用于 ingestion 的
  collection token。
- **浏览测试** —— 按状态过滤（`all`、`failing`、`passing`、`muted`、
  `skipped`），按名字/文件/标签搜索，按最近、名字、最慢或失败数排序。
- **检查测试** —— 查看它的元数据、聚合历史和执行行。
- **改变测试状态** —— mute 或 skip 一个测试（`PATCH
  /api/test-engine/tests/{id}/state`），或重新启用它。
- **管理 collection tokens** —— 读取和吊销套件的 token（`GET`/`POST`/`DELETE`
  token 端点）。

控制台里的每个动作都一一对应
[Test Engine 管理 API](/zh-hans/api/test-engine/) 里的一个端点。

## 实例管理后台

自托管管理员可以通过 `SLASH_ADMIN_SECRET_PATH` 开启独立的 `/admin` 运维后台。
它使用自己的 20 分钟会话，不会因为某个 Slash 用户或组织角色而自动授权。未配置
secret 时，页面和 API 都返回 404。

后台显示安装数量、最近的 webhook delivery 及转义后的 JSON payload、相关 slash
command 响应和队列诊断。只有管理员点击 **Refresh from GitHub** 时才会校准安装列表；
跨副本数据库锁和 5 分钟冷却会避免重复请求 GitHub App API。第一版不会重试或删除
失败的 webhook。
